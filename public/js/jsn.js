$(document).ready(function () {

  /* Get request for getting an article */
  $("#articles-button").click(function (e) {
    e.preventDefault();
  });

  /* Get request for getting a user */
  $("#user-button").click(function (e) {
    e.preventDefault();
  });

  /* Get request for getting a post */
  $("post-button").click(function (e) {
    e.preventDefault();
  });

});